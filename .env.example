# ============================================================
# I-SPOT Environment Variables
# 실제 API Key는 이 파일에 작성하지 않는다.
# 로컬에서는 .env 파일을 생성하여 실제 값을 설정한다.
# ============================================================


# ------------------------------------------------------------
# STT Provider
# ------------------------------------------------------------
# mock      : API 없이 로컬 테스트
# deepgram  : 현재 메인 STT
# whisper   : OpenAI Whisper 기반 STT
# clova     : CLOVA Speech 연동
I_SPOT_STT_PROVIDER=deepgram

# 공통 STT API Key / URL
# provider별 설정에 따라 선택적으로 사용
I_SPOT_STT_API_KEY=
I_SPOT_STT_API_URL=

# 현재 Deepgram 사용 시 필요
DEEPGRAM_API_KEY=


# ------------------------------------------------------------
# Speaker Diarization
# ------------------------------------------------------------
# 별도 diarization provider를 지정할 경우 사용
# 현재 Deepgram 자체 diarization을 사용하는 경우
# 별도 설정이 필요하지 않을 수 있음.
I_SPOT_SPEAKER_DIARIZATION_PROVIDER=
I_SPOT_SPEAKER_DIARIZATION_MODEL=


# ------------------------------------------------------------
# OpenAI / Whisper
# ------------------------------------------------------------
# OpenAI 기반 기능을 사용할 경우 설정
OPENAI_API_KEY=


# ------------------------------------------------------------
# Groq / LLM Analyzer (Optional)
# ------------------------------------------------------------
# ispot_analyzer.py의 Groq 기반 기능을 사용할 경우에만 설정
GROQ_API_KEY=


# ------------------------------------------------------------
# Pyannote Experiment (Optional / NOT production)
# ------------------------------------------------------------
# Pyannote diarization 실험을 재현할 경우에만 사용
# 현재 메인 STT pipeline에는 Pyannote를 사용하지 않음.
PYANNOTE_AUTH_TOKEN=