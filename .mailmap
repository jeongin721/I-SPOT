# 동일 인물의 여러 커밋 신원을 하나로 묶는다.
#
# 형식:  올바른이름 <올바른메일> 커밋에찍힌이름 <커밋에찍힌메일>
#
# git shortlog / git log %aN 가 이 파일을 참고한다.
# 이력을 다시 쓰지 않으므로 이미 받아간 팀원에게 영향이 없다.

# 9/3 통합 작업은 mingyu 가 Cursor 로 진행했고, 커밋 저작자가
# 도구 이름(Cursor Agent)으로 기록되었다.
mingyu <hedcoa1@gmail.com> Cursor Agent <cursoragent@cursor.com>
